[
  {
    "name": "composer",
    "version": "0.10.0-dev",
    "extensionSet": true,
    "categories": [
      "Misc"
    ],
    "author": "Tetrate",
    "featured": true,
    "description": "Extension set of all in-tree golang dynamic module extensions and Go plugin loader",
    "longDescription": "The Composer is a dynamic module that bundles all in-tree Go extensions and\nthe Go plugin loader into a single shared library (libcomposer.so).\n\n## Packaging\n\nComposer ships as an OCI image containing the compiled shared library. It can\nbe built in two variants:\n\n- **Full**: Includes all embedded plugins and the Go plugin loader.\n- **Lite**: Includes only the Go plugin loader for runtime-loaded plugins.\n",
    "type": "composer",
    "filterType": [
      "http"
    ],
    "tags": [
      "go",
      "dynamic-module",
      "plugin-loader"
    ],
    "license": "Apache-2.0",
    "examples": [
      {
        "title": "Run with the example plugin",
        "description": "Start Envoy with the composer dynamic module and example plugin",
        "code": "boe run --extension example\n"
      }
    ],
    "minEnvoyVersion": "1.38.0",
    "maxEnvoyVersion": "1.39.0",
    "composerVersion": "0.10.0-dev",
    "sourcePath": "composer"
  },
  {
    "name": "dns-gateway",
    "version": "0.1.0",
    "extensionSet": true,
    "categories": [
      "Network",
      "Security"
    ],
    "author": "Palantir Technologies",
    "description": "Transparent egress routing — intercept DNS and route TCP by domain",
    "longDescription": "Transparent egress routing for Envoy. Applications make normal DNS lookups and TCP\nconnections — this extension set intercepts both so Envoy can control which upstream cluster\nhandles each domain, attach metadata (auth tokens, policy IDs), and enforce routing\npolicy, all without any application changes.\n\n## Use cases\n\n* Route outbound traffic through different upstream proxies based on destination domain\n* Attach per-domain credentials or policy metadata to proxied connections\n* Enforce egress access control by selectively intercepting DNS for specific domains\n\n![img](/extensions/dns-gateway.png)\n\n## Extensions\n\nThe DNS Gateway is a pair of filters that work together, both provided by the same\n`dns-gateway` dynamic module and sharing a process-wide virtual-IP cache:\n\n* **[resolver](resolver)** — a UDP listener filter that intercepts DNS\n  queries for configured domain patterns, allocates a virtual IP from a dedicated CIDR range,\n  and responds with an A record.\n* **[lookup](lookup)** — a network filter that, when the application\n  opens a TCP connection to a virtual IP, looks up the original domain and its configured\n  metadata from the shared cache and exposes them as Envoy filter state.\n\n## Backward compatibility\n\nThe combined `dns-gateway` extension is preserved: the legacy invocation\n`--extension dns-gateway --filter-type udp_listener` (and `--filter-type network`) still works\nand resolves to the same filters as the split extensions above.\n\n## Prerequisites\n\nRequires iptables/nftables rules to redirect application traffic to Envoy:\n- **DNS**: UDP port 53 redirected to Envoy's DNS listener\n- **TCP**: Outbound connections to virtual IP ranges redirected to Envoy's TCP listener\n",
    "type": "rust",
    "filterType": [
      "udp_listener",
      "network"
    ],
    "tags": [
      "access-control",
      "dns",
      "egress",
      "networking",
      "routing",
      "rust",
      "security",
      "transparent-proxy",
      "virtual-ip"
    ],
    "license": "Apache-2.0",
    "examples": [],
    "minEnvoyVersion": "1.38.0",
    "maxEnvoyVersion": "1.39.0",
    "sourcePath": "dns-gateway"
  }
]