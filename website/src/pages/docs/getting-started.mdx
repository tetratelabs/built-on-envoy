---
layout: ../../layouts/DocsLayout.astro
title: Getting Started
description: Get started with Built On Envoy CLI
---

# Getting started

The <code>boe</code> (Built On Envoy) CLI provides a zero-friction developer experience for discovering, running, and building Envoy® Proxy extensions.

## Installation

Choose your preferred installation method:

### Install script

```bash
curl -sL https://builtonenvoy.io/install.sh | sh
```

### Build from source

```bash
git clone https://github.com/tetratelabs/built-on-envoy
cd built-on-envoy/cli
make
```

## Quick start

Once installed, you can immediately start using extensions:

### Discover available extensions

Discover all publicly available extensions:

```shell
boe list
```

### Run extensions

Easily run any extension. The `boe` CLI will automatically download Envoy for your operating
system and architecture, and start it preconfigured with the requested extensions and a test
upstream:

```shell
boe run --extension example-lua --extension cache
```

Or run extensions directly from a local directory:

```shell
boe run --local ./my-extension
```

### Generate Envoy configuration

Inspect the Envoy configuration so that you can replicate it in your environment:

```shell
boe gen-config --extension example-lua --local ./my-extension >envoy.yaml
```

## Next steps

- Explore the CLI Commands:
  - [boe list](/docs/cli/list) - List available extensions
  - [boe run](/docs/cli/run) - Run Envoy with extensions
  - [boe gen-config](/docs/cli/gen-config) - Generate Envoy configuration
- [Create your first extension](/docs/writing-go-extensions)
- Browse the [Extensions Catalog](/extensions) to find extensions for your use case
- Check the [Extension Manifest Reference](/docs/reference/manifest) to create your own extensions
