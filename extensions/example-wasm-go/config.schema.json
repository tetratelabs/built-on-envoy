{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "example-wasm-go/config.schema.json",
  "title": "example-wasm-go Configuration",
  "description": "Configuration for the example-wasm-go extension.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "header_value": {
      "type": "string",
      "description": "Custom value for the header to add. Defaults to \"example\" if not set."
    }
  }
}
