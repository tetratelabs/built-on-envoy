# Copyright Built On Envoy
# SPDX-License-Identifier: Apache-2.0
# The full text of the Apache license is available in the LICENSE file at
# the root of the repo.

name: openfga
parent: composer
categories:
  - AI
  - Security
author: Tetrate
description: Inline OpenFGA authorization for Envoy, Envoy Gateway, and AI Gateway — no sidecar required
longDescription: |
  This extension implements [OpenFGA](https://openfga.dev/) authorization checks as an
  inline Envoy HTTP filter. It calls the OpenFGA Check API via Envoy's async HTTP callout
  mechanism to determine whether a request should be allowed or denied, without requiring
  a separate authorization sidecar.

  Works with [Envoy](https://www.envoyproxy.io/), [Envoy Gateway](https://gateway.envoyproxy.io/),
  and [Envoy AI Gateway](https://aigateway.envoyproxy.io/) (AIGatewayRoute, HTTPRoute, MCPRoute).
  The extension extracts user, relation, and object from request headers, URL path segments,
  query parameters, or static values, then delegates the authorization decision to OpenFGA.

  ## Key Features

  - **Async HTTP callout**: Uses Envoy's built-in `HttpCallout` to call the OpenFGA Check API
    through a configured cluster, integrating with Envoy's connection pooling and circuit breaking.
  - **Flexible input mapping**: Extract `user`, `relation`, and `object` from request headers,
    URL path segments, query parameters, or static values — no ExtProc needed for REST APIs.
  - **AI Gateway ready**: Works in the same Envoy filter chain as the AI Gateway External
    Processor. Use it to control access to AI models, MCP tools, or any HTTP resource.
  - **Dry-run mode**: Log authorization decisions without enforcing them.
  - **Fail-open mode**: Allow requests through when the OpenFGA server is unreachable.

  ## Configuration

  The extension requires an Envoy cluster pointing to the OpenFGA server. The `user`, `relation`,
  and `object` fields for the Check API are extracted from request headers or set as static values.

  ```json
  {
    "cluster": "openfga",
    "openfga_host": "openfga.default.svc:8080",
    "store_id": "01ABCDEF",
    "user": {"header": "x-user-id", "prefix": "user:"},
    "relation": {"value": "can_use"},
    "object": {"header": "x-ai-model", "prefix": "model:"}
  }
  ```

  ## Multi-Rule Configuration

  For routes that handle multiple resource types (e.g., AI models, MCP tools, and generic HTTP
  resources), use the `rules` array instead of the legacy single-rule config. Each rule can match
  on request headers and define its own `relation` and `object`.

  - **`rules`**: Array of rules evaluated in order. The first matching rule is used. A rule with no
    `match` or empty `match.headers` is a catch-all and must be last.
  - **`match.headers`**: Object mapping header names to match values. Use `"*"` to require the
    header to be present with any non-empty value; use an exact string for a value match.
  - **Top-level `user`**: Serves as the default user source for all rules. Individual rules can
    override with their own `user` field.

  Example: AI model rule (header `x-ai-eg-model`), MCP tool rule (header `x-mcp-tool`), and a
  catch-all for generic resources.

  ```json
  {
    "cluster": "openfga",
    "openfga_host": "openfga.default.svc:8080",
    "store_id": "01ABCDEF",
    "user": {"header": "x-user-id", "prefix": "user:"},
    "rules": [
      {"match": {"headers": {"x-ai-eg-model": "*"}}, "relation": {"value": "can_use"}, "object": {"header": "x-ai-eg-model", "prefix": "model:"}},
      {"match": {"headers": {"x-mcp-tool": "*"}}, "relation": {"value": "can_invoke"}, "object": {"header": "x-mcp-tool", "prefix": "tool:"}},
      {"relation": {"value": "can_access"}, "object": {"header": "x-resource-id", "prefix": "resource:"}}
    ]
  }
  ```

  ### Configuration Reference

  | Field | Type | Required | Default | Description |
  |-------|------|----------|---------|-------------|
  | `cluster` | string | yes | | Envoy cluster name routing to the OpenFGA server |
  | `openfga_host` | string | yes | | Hostname for the `Host` header in callouts |
  | `store_id` | string | yes | | OpenFGA store ID |
  | `authorization_model_id` | string | no | | OpenFGA authorization model ID (uses latest if omitted) |
  | `user` | valueSource | yes* | | How to extract the user for the Check call (*required when using `rules`) |
  | `relation` | valueSource | yes* | | How to extract the relation (*when not using `rules`) |
  | `object` | valueSource | yes* | | How to extract the object (*when not using `rules`) |
  | `rules` | array | no | | Multi-rule config; each rule has `match`, `relation`, `object`, optional `user` |
  | `match` | object | no | | Per-rule: `headers` map (header name → `"*"` or exact value) |
  | `fail_open` | bool | no | `false` | Allow requests when OpenFGA is unreachable |
  | `dry_run` | bool | no | `false` | Log decisions without enforcing them |
  | `timeout_ms` | int | no | `5000` | HTTP callout timeout in milliseconds |
  | `deny_status` | int | no | `403` | HTTP status code for denied requests |
  | `deny_body` | string | no | `"Forbidden"` | Response body for denied requests |
  | `metadata` | object | no | | Optional. When set, writes the authorization decision to dynamic metadata for downstream filters. Requires `namespace` and `key`. |

  ### valueSource Reference

  Each of `user`, `relation`, and `object` (and per-rule overrides) is a `valueSource` object.
  Exactly one of `value`, `header`, `path_segment`, or `query_param` must be set.

  | Field | Type | Description |
  |-------|------|-------------|
  | `value` | string | Static value used as-is |
  | `header` | string | Read value from this request header |
  | `path_segment` | int | Extract the URL path segment at this 0-based index. Negative values count from the end: `-1` is the last segment. The query string is stripped before indexing. |
  | `query_param` | string | Extract the first value of this named query parameter from the URL. URL-encoding is decoded automatically. |
  | `prefix` | string | Prepend this string to the extracted value |

  ## Filter Chain Ordering

  When deploying with Envoy AI Gateway, filter chain order determines which headers are
  available when the openfga filter runs.

  **Recommended order:**

  1. **JWT Authentication** (Envoy built-in) — validates the token and optionally injects
     claims as request headers via `SecurityPolicy.jwt.claimsToHeaders`
  2. **jwe-decrypt** (optional) — decrypts a JWE-wrapped token before JWT authn processes it
  3. **openfga** — reads user identity from a header, checks authorization against OpenFGA
  4. **AI Gateway ExtProc** — parses the request body to route to the correct AI model

  **Why openfga before the AI Gateway ExtProc?**

  OpenFGA is a header-only filter: it runs on `OnRequestHeaders` before the request body is
  read. The AI Gateway ExtProc processes the request body and may set headers like
  `x-ai-eg-model` based on the JSON payload. When openfga runs first, it reads
  `x-ai-eg-model` as set by the **client** — which is the intended control point for
  access control (the client declares what it wants, and the check runs before any
  processing). If the model name must come from the body-parsed value injected by ExtProc,
  place openfga **after** the AI Gateway ExtProc in the filter chain instead.

  ## Identity Integration

  **Envoy Gateway JWT claims injection (recommended)**

  The simplest way to pass user identity to openfga is via Envoy Gateway's `SecurityPolicy`
  with `claimsToHeaders`. Envoy validates the JWT and injects any claim (e.g. `sub`) into a
  request header before the filter chain runs — no extra plugins needed. The header name is
  fully configurable; examples here use `x-user-id` for consistency with the rest of the
  Composer extension ecosystem.

  ```yaml
  apiVersion: gateway.envoyproxy.io/v1alpha1
  kind: SecurityPolicy
  metadata:
    name: jwt-policy
  spec:
    targetRef:
      group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: my-route
    jwt:
      providers:
        - name: my-provider
          issuer: https://auth.example.com
          remoteJWKS:
            uri: https://auth.example.com/.well-known/jwks.json
          claimsToHeaders:
            - claim: sub
              header: x-user-id
  ```

  Then configure openfga to read that header:
  ```json
  {"header": "x-user-id", "prefix": "user:"}
  ```

  **jwe-decrypt + JWT authn (for encrypted tokens)**

  For deployments where clients send JWE-encrypted JWTs, chain the `jwe-decrypt` plugin
  before openfga in the same Composer filter. The `jwe-decrypt` plugin decrypts the
  `Authorization` header in-place; Envoy's JWT authn filter then validates the inner JWT
  and `claimsToHeaders` makes the `sub` claim available as `x-user-id` for openfga to read.

  ## Metrics

  The extension exposes:

  - **`openfga_requests_total`** (counter, tag: `decision`): Authorization outcomes
  - **`openfga_check_duration_ms`** (histogram): Latency of OpenFGA Check API calls in milliseconds

  | Decision | Description |
  |----------|-------------|
  | `allowed` | The Check API allowed the request. |
  | `denied` | The Check API denied the request. |
  | `failopen` | An error occurred and the request was allowed because fail-open is enabled. |
  | `dryrun_allow` | The request was denied but allowed because dry-run is enabled. |
  | `error` | An error occurred and the request was denied. |
type: go
tags:
  - go
  - http
  - filter
  - openfga
  - security
  - authorization
  - ai-gateway
license: Apache-2.0
examples:
  - title: AI Model Access Control
    description: |
      Control which users can access specific AI models through an AIGatewayRoute.
      The user identity comes from a header set by upstream authentication, and the
      model name from a header set by the AI Gateway ExtProc.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "relation": {"value": "can_use"},
        "object": {"header": "x-ai-model", "prefix": "model:"}
      }'

      # Allowed (if user:alice has can_use relation with model:gpt-4 in OpenFGA)
      curl -H "x-user-id: alice" -H "x-ai-model: gpt-4" http://localhost:10000/v1/chat/completions

      # Denied
      curl -H "x-user-id: bob" -H "x-ai-model: gpt-4" http://localhost:10000/v1/chat/completions
  - title: MCP Tool Access Control
    description: |
      Control which users can invoke specific MCP tools through an MCPRoute.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "relation": {"value": "can_invoke"},
        "object": {"header": "x-mcp-tool", "prefix": "tool:"}
      }'
  - title: Multi-Route Authorization
    description: |
      Handle AI model access and MCP tool access on the same Gateway with a
      single filter instance. Rules are evaluated in order; the first match wins.
      A catch-all rule at the end covers generic HTTP resources.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "rules": [
          {
            "match": {"headers": {"x-ai-eg-model": "*"}},
            "relation": {"value": "can_use"},
            "object": {"header": "x-ai-eg-model", "prefix": "model:"}
          },
          {
            "match": {"headers": {"x-mcp-tool": "*"}},
            "relation": {"value": "can_invoke"},
            "object": {"header": "x-mcp-tool", "prefix": "tool:"}
          },
          {
            "relation": {"value": "can_access"},
            "object": {"header": "x-resource-id", "prefix": "resource:"}
          }
        ]
      }'

      # AI model request (matches rule 1)
      curl -H "x-user-id: alice" -H "x-ai-eg-model: gpt-4" http://localhost:10000/v1/chat/completions

      # MCP tool request (matches rule 2)
      curl -H "x-user-id: alice" -H "x-mcp-tool: github__issue_read" http://localhost:10000/mcp

      # Generic HTTP request (matches catch-all rule 3)
      curl -H "x-user-id: alice" -H "x-resource-id: planning" http://localhost:10000/api/docs
  - title: Dry-Run Mode
    description: |
      Log authorization decisions without enforcing them.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "relation": {"value": "can_access"},
        "object": {"header": "x-resource", "prefix": "document:"},
        "dry_run": true
      }'
  - title: Fail-Open Mode
    description: |
      Allow requests when the OpenFGA server is unreachable.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "relation": {"value": "can_access"},
        "object": {"header": "x-resource", "prefix": "document:"},
        "fail_open": true
      }'
  - title: REST Resource Authorization via URL Path
    description: |
      Authorize access to REST resources identified by URL path segment — no custom
      headers or ExtProc needed. For an API at `/api/documents/{docId}`, use
      `path_segment: 2` to extract the document ID from the third path segment (0-indexed).
      Negative values count from the end: use `path_segment: -1` to always get the last
      segment, regardless of path depth.
    code: |
      boe run --local . --config '{
        "cluster": "openfga",
        "openfga_host": "localhost:8080",
        "store_id": "YOUR_STORE_ID",
        "user": {"header": "x-user-id", "prefix": "user:"},
        "relation": {"value": "can_access"},
        "object": {"path_segment": 2, "prefix": "document:"}
      }'

      # GET /api/documents/planning -> checks user:alice can_access document:planning
      curl -H "x-user-id: alice" http://localhost:10000/api/documents/planning

      # GET /api/documents/budget -> checks user:alice can_access document:budget
      curl -H "x-user-id: alice" http://localhost:10000/api/documents/budget

      # Using -1 to always capture the last segment (works for /docs/budget or /api/v2/docs/budget)
      # object: {"path_segment": -1, "prefix": "document:"}
